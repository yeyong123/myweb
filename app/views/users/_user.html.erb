<li>
  <%= gravatar_for user  %>
  <%= link_to user.name, user %>
   <% if current_user.admin? && !current_user?(user) %>
     | <%= link_to "删除", user, method: :delete, confirm: "确定删除？" %>
    <% end %>
</li>
